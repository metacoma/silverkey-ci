pipeline {
    agent {
        docker { image 'vookimedlo/ubuntu-qt:5.10_clang_xenial' }
    }
    stages {
        stage('Build') {
            steps {
                sh 'id'
            }
        }
    }
}
