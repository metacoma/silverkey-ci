pipeline {
    
    agent {
      dockerfile {
        dockerfile true
      
      } 
     }
    stages {
        stage('Build') {
            steps {
                sh 'id'
            }
        }
    }
}
